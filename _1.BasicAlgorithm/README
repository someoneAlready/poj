POJ 3096 Surprising Strings
给定一个字符串，对于0间隔的字符对都一一不同，则这个字符对事0-unique的，如果对于所有的D，这个字符串都是D-unique的，那么这个串就是惊喜的。给定一个串判断它是否惊喜。
模拟+map 枚举所有的D，然后对于当前的D枚举所有间隔为D的字符对，用map判断是否有相同的字符对。

POJ 3007 Organize Your Train part II
给定一个字符串，可以把这个字符串分成任意两段，这两段可以反转或者不放转，然后将两段字符串，不分次序的拼接在一起，求该字符串可以生成多少中不同的串。
模拟+hash 模拟出不同的两段子串，然后反转，生成新的串进行hash。

POJ 3393 Lucky and Good Months by Gregorian Calendar
如果一个月的最后一个工作日是星期五，则这个月就是幸运月，如果这个月的第一个工作日是星期一，则这个月就是好月，给定开始的年月和结束的年月，求有多少个幸运月和多少个好月。
模拟 根据题意循环算出1-10000年每个月的第一天和最后一天是星期几，然后求出改天是否事幸运月和好月。

POJ 1472 Instant Complexity
给定一个程序计算该程序的多项式时间复杂度。
模拟 根据给定程序的递归结构，递归计算时间复杂度。

POJ 3371 Flesch Reading Ease
给定一篇文章，计算它的音节数、单词数、句子数，然后通过给定的公式计算这篇文章的阅读难度系数。
模拟 通过给定的分句符号来计算句子，和给定的规则计算一个单词的音节数。

POJ 1027 The Same Game
一个棋盘有RGB三种颜色的棋子，每次消去同样颜色连续且数目大于1的棋子，且每次要找数量最多，最左和最下的棋子来消除。求消除步骤。
模拟 每次从左到右，从下到上找到数量最多的棋子来消除。

POJ 2706 Connect
给定一个棋盘，左右为黑子，上下为白子的基地，每次轮流放子，如果已有的子和同颜色的子可以走一步马子步，且与已有的线不相交，则可连一条线，如果最后黑子将左右联通，白子将上下联通，则该子获胜。求最后黑子的一步是否获胜。
模拟+判断线段相交 枚举每一次生成的点和线，然后判断相交，最后dfs，是否能从左遍历到最右。
