POJ 3411 Paid Roads
给定一个最多有10个节点的有向带权网络，每条a到b的有向边有两个权值，一个是在之前经过c点可付费pi，一个是经过b点之后在付费ri，就是说如果一条路径在之前已经经过c点，那么这条边的权值就为pi，否则为ri，要求从1到N求经过的最短权值。
状态压缩的记忆化搜索的最优化剪枝 f[k][i]表示从1节点出发，已经经过k的二进制编码为1的节点，且当前在i节点经过的最小权值，如果dfs(k, i)的权值大于等于记忆化搜索得到的f[k][i]的权值，便可直接剪去该搜索分支。

POJ 1724 ROADS
给定一个节点数最多有100的有向带权网络，每条边有两个权值，一个是边的长度，一个是通过该边需要的钱数，给定你一定的钱数K，要求使用不多于K的钱数，从1到N能走到的最短路径。
最优化剪枝+可行性剪枝的搜索 求出每个节点到n的钱和长度的最短路，即求所有边的反向边所构成的新图，节点n的钱和长度的单源最短路。然后进行搜索，如果搜索到当前节点i的长度+i到n的最短长度还是大于或等于已求出的可行解，则剪枝；如果当前节点i的钱+i到n的钱的最短路大于K，则剪去该搜索分支；

POJ 3373 Changing Digits
给定一个长度不大于100的数字串s，和一个不大于10000的整数k，要求该数字串s替换最少的数字成为k的倍数，如果有多个替换相同的最少的数字的解，则给出数值最小的解。
记忆化搜索 由于k不大于10000，所以最多改变4个数字即可得到k的倍数。搜索dfs(i, j, k)为当前为第i个数字，还需要改变j个数字，且前i-1数字对K取余为k；最终的合法状态为dfs(n, 0, 0)，即整个串对K取余为0，即为K的倍数，且已经改变了需要改变的数字。每次搜索从小到大枚举改变的数字，如果搜索到可行解，便停止搜索，因为当前的解的数值最小。

POJ 1691 Painting A Board
在一个N*M的矩形上最多有15个互相不覆盖的小矩形，每个矩形都有一定的颜色，每一次可以个某同一种颜色的矩形上色，且该矩形上方的矩形都已经上色。求将所有矩形上色的最少操作次数。
状态压缩的记忆化搜索 对于状态k，表示k的二进制为1其颜色还未上，f[k]表示要把状态k的矩形都上色需要最小的操作数。

