POJ 1191 棋盘分割
给定一个8*8的棋盘，每个格子里有一定的权值，要求分割n-1次，每次分割后等到两块棋盘，一块棋盘保留，另一块棋盘继续分割，这样就等到了n块棋盘，求如何分割等到的n块棋盘的权值之和的方差最小，求出最小方差。
DP 方差为sqrt( sum(x[i]- X)^2, i=1..n ), X为x[i]的平均值，即为整个棋盘权值之和除以n，f[I][i][j][x][y]表示I-1一次切割，有I个矩形 sum_(i=1..I)(x[i]-X)^2的最小值，枚举横竖切割线进行DP方程的状态转移。
	f[I][i][j][x][y]=Min{ 
				f[I-1][i][j][xx][y]  + (s[xx+1][j][x][y]-X)^2, 
				(s[i][j][xx][y]-X)^2 + f[I-1][xx+1][j][x][y], 
				f[I-1][i][j][x][yy]  + (s[i][yy+1][x][y]-X)^2,
				(s[i][j][x][yy]-X)^2 + f[I-1][i][yy+1][x][y]
	}	

POJ 1054 The Troublesome Frog
给定一个R*C的棋盘，棋盘上有一些点，求棋盘求点数最多的一条点，该直线上每个点的间隔是一样的。
枚举 枚举该直线前端的两个点，求得该直线的点数，取直线点数中的最大值。

POJ 3280 Cheapest Palindrome
给定一个长度为n<=2000的字符串，可以把该字符串插入一个字符或删除一个字符，不过都需要一定的费用，要求最小的费用使该字符串成为一个回文串。
将该字符串求逆序串，与原串求权值最大的公共子序列，对于一个没匹配的字符，可以


POJ 2029 Get Many Persimmon Trees
给定一个W*H的矩阵，矩阵的每一个元素为0或为1，给定S<=W, T<=H，求一个S*T的矩形，要求S*T中1的元素最多。
DP 枚举S*T矩阵的第一个元素，利用容斥原理求出该矩阵中1的元素个数，取所有矩阵的最大值。

POJ 2948 Martian Mining
一个N*M的矩阵有都分别有一定数量的矿产a、b，要在该矩阵建立传输线，a矿产只能运往最最左边，b矿产只能运往最上方，且每个元素只能建立一个方向的运输线，求最左方和最上方最多可以加工多少矿产
DP f[i][j]表示在i行的前j的a矿产是运往左边的，所以j+1到m的矿产是运往上方的，即在i-1行，j+1到m的矿产一定是运往上方的，所以
	f[i][j]=aSum[i][1..j]+bSum[i][j+1..m]+max{f[i-1][k] | 0<=k<=j}

POJ 1925 Spiderman

POJ 3034 Whac-a-Mole

POJ 3254 Corn Fields

POJ 2411 Mondriaan's Dream

POJ 1185 炮兵阵地


POJ 2057 The Lost House


POJ 1947 Rebuilding Roads

POJ 2486 Apple Tree
给定一个N个节点的树，求最少删除几条边可以生成一个有p个节点的子树。

POJ 3140 Contestants Division
