POJ 1191 棋盘分割
给定一个8*8的棋盘，每个格子里有一定的权值，要求分割n-1次，每次分割后等到两块棋盘，一块棋盘保留，另一块棋盘继续分割，这样就等到了n块棋盘，求如何分割等到的n块棋盘的权值之和的方差最小，求出最小方差。
DP 方差为sqrt( sum(x[i]- X)^2, i=1..n ), X为x[i]的平均值，即为整个棋盘权值之和除以n，f[I][i][j][x][y]表示I-1一次切割，有I个矩形 sum_(i=1..I)(x[i]-X)^2的最小值，枚举横竖切割线进行DP方程的状态转移。
	f[I][i][j][x][y]=Min{ 
				f[I-1][i][j][xx][y]  + (s[xx+1][j][x][y]-X)^2, 
				(s[i][j][xx][y]-X)^2 + f[I-1][xx+1][j][x][y], 
				f[I-1][i][j][x][yy]  + (s[i][yy+1][x][y]-X)^2,
				(s[i][j][x][yy]-X)^2 + f[I-1][i][yy+1][x][y]
	}	

POJ 1054 The Troublesome Frog
给定一个R*C的棋盘，棋盘上有一些点，求棋盘求点数最多的一条点，该直线上每个点的间隔是一样的。
枚举 枚举该直线前端的两个点，求得该直线的点数，取直线点数中的最大值。

POJ 3280 Cheapest Palindrome
给定一个长度为n<=2000的字符串，可以把该字符串插入一个字符或删除一个字符，不过都需要一定的费用，要求最小的费用使该字符串成为一个回文串。
权值最大的公共子序列 将该字符串求逆序串，与原串求权值最大的公共子序列，对于一个没匹配的字符，可以在相对的串插入该字符，或者把该字符串删除都可构成回文串，所以对于每个不匹配的字符，选择插入、删除费用最小的进行操作。最后用串的总费用 - 权值最大的公共之序列 便是要话费的费用。

POJ 2029 Get Many Persimmon Trees
给定一个W*H的矩阵，矩阵的每一个元素为0或为1，给定S<=W, T<=H，求一个S*T的矩形，要求S*T中1的元素最多。
DP 枚举S*T矩阵的第一个元素，利用容斥原理求出该矩阵中1的元素个数，取所有矩阵的最大值。

POJ 2948 Martian Mining
一个N*M的矩阵有都分别有一定数量的矿产a、b，要在该矩阵建立传输线，a矿产只能运往最最左边，b矿产只能运往最上方，且每个元素只能建立一个方向的运输线，求最左方和最上方最多可以加工多少矿产
DP f[i][j]表示在i行的前j的a矿产是运往左边的，所以j+1到m的矿产是运往上方的，即在i-1行，j+1到m的矿产一定是运往上方的，所以
	f[i][j]=aSum[i][1..j]+bSum[i][j+1..m]+max{f[i-1][k] | 0<=k<=j}

POJ 1925 Spiderman
在一条道路有n个建筑，蝙蝠侠在建筑1，它的目标在建筑n，蝙蝠侠可通过射蜘蛛丝到一个建筑，然后在到另一个建筑，不过蜘蛛丝不能大于建筑的高度，否则蜘蛛侠会落在地上，要求射的蜘蛛丝最少就可以到达目的地。
DP 通过每个建筑射蜘蛛丝，可以使在距离建筑左端为X米，到达到建筑右段x米。而对于每个建筑射向它的蜘蛛丝的x坐标（y坐标一定，都为建筑1的高度）是有限制的即蜘蛛丝的长度不超过建筑的高度。由上述信息可知，蜘蛛侠在空中停靠的x坐标一定为整数，所以对这些空中停留的坐标进行DP，DP[i]，就表示蝙蝠下停留在i时射的最少的蜘蛛丝，通过每个建筑对状态进行更新。 
		                   DP[2*x-i] = min { DP[2*x-i], DP[i]+1 }
对于状态大于等于n建筑的坐标即为一个解，取解的最小值。

POJ 3034 Whac-a-Mole
一共有t个时刻，每个时刻可以移动你的锤子从原来的坐标到任意一个坐标，在这条移动的直线上，在该时段出现的老鼠都会被达到，求如何移动锤子可以打到最多的老鼠。
DP f[t][i][j]表示第t秒，锤子移动到i，j时敲到的最多的老鼠。算法的负责度为O（T*N^4)	f[t][i][j]=max{f[t-1][x][y]}, i-5<=x<=i+5

POJ 3254 Corn Fields
给定一个N*M（1<=N,M<=12）的矩阵，矩阵的元素为0或者为1，表示这里可以种小麦，不过没相邻的地方不能都种小麦，求做最多种多少小麦。
状态压缩DP 利用位运算表示一行已经种的小麦，一行已经种的小麦一定为矩阵当前行的子集，且与自身位左移的与运算为0，且与上一行的与运算也为0。

POJ 2411 Mondriaan's Dream
给定一个N*M（1<=N,M<=11）的矩阵，要求给用1*2或2*1的小块矩阵填满大矩阵，求有多少方案。


POJ 1185 炮兵阵地
一个炮兵攻击的范围是竖直和水平距离两个单位长度，现在给定一个N*M的01矩阵，1表示该地点能放炮兵，要求放的炮兵不能互相攻击，求该矩阵能放炮兵的最多数。
状态压缩DP 该行的状态有前两行的状态共同决定，该行的状态r一定为该矩阵01的子集，且与自身位左移一位和左移两位做与运算都为0，且与前两行的状态s，t做与运算也为0， f[(s<<m)+r]+=f[(t<<m)+s]

POJ 2057 The Lost House
给定一个N个节点的树，蜗牛原来在树的某个叶子上睡觉，不过后来它从它的壳里掉了出来，现在它要从新从根节点1寻找它的壳。有些的节点上有虫子，可以告诉蜗牛他是否经过这个节点，求这蜗牛找到它的壳的最小数学期望。

POJ 1947 Rebuilding Roads
给定一个N个节点的树，求最少删除几条边可以生成一个有p个节点的子树。

POJ 2486 Apple Tree
给定一个n个节点的树，树上每个节点都有一定的苹果，要求从根节点1出发，只能走K步，能吃到的最多的苹果数。


POJ 3140 Contestants Division
给定一颗N个节点的树，要求把这颗树上分成两个部分，要求两个部分节点的权值之和差的绝对值最小。
