POJ 2488 A Knight's Journey
给定一个n*m<=26的棋盘，要求骑士遍历棋盘上的所有点、且每个点仅遍历一次，求遍历轨迹字典序最小的轨迹序列。
dfs 由于要求字典序最小，所有用dfs求出最优解。

POJ 3083 Children of the Candy Corn
给定一个n*m的迷宫,n,m<=40，迷宫有出口S、入口E，求沿着入口左墙走、沿着入口右墙走的路径长度是多少，和最短路径是多少。
dfs+bfs dfs求沿着入口左墙走、沿着入口右墙走的路径长度，求沿着左墙走优先向左转、然后是直走、然后右转、然后后退，bfs求最短路径长度。

POJ 3009 Curling 2.0
给定一个n*m的棋盘，n,m<=20，棋盘上有起点、砖块、目标，求不超过10次的最小移动次数碰到目标。
dfs 深度优先搜索+最优性剪枝

POJ 1321 棋盘问题
给定一个n*n的棋盘，n<=8，棋盘上有些地方能放棋子，有些地方不能放棋子，要放k个棋子，且每行每列只能放一个棋子，求放棋子的方法数。
dfs 枚举棋盘的布局。

POJ 2551 Dungeon Master
给定一个3D的n*m*l迷宫，求最少步数走出迷宫
bfs 广度优先搜索求得最小步数

POJ 3278 Catch That Cow
给定一个数轴，起点是N，终点是K，可以向前或向后各走一步，或者走到当前位置的两倍处，求最小步数走到终点。
bfs 广度优先搜索求得最小步数

POJ 1426 Find The Multiple
给定一个n<=200，求n的任意一个倍数m<=10^100，且m中只有1和0
dfs 深度优先搜索m，不断在当前m后面添加0或1，运用取余运算计算m是否是n的倍数。

POJ 3126 Prime Path
给定两个4位的素数a，b，求最少的次数改变4位素数a的某一位使它变成b，且中间过程也为素数。
bfs 广度优先搜索求最小次数。

POJ 3087 Shuffle'm Up
给定大小为C的两堆牌，求多少次洗牌后可得到目标状态
hash+模拟 因为可能无解，所以用hash来判断是否回到原来的状态。

POJ 3414 Pots
给定容积为A、B的两个壶，可以倒满任意一个壶，可以倒空任意一个壶，可以从A、B壶互相倒，求最短的操作次数，和操作序列
bfs 广度优先搜索求最小操作次数，并且记录操作过程。

